[tool.ruff]
line-length = 100
extend-exclude = [".venv", "data", ".pytest_cache"]

[tool.ruff.lint]
# Default ruff rule set: pycodestyle errors (E4/E7/E9) + Pyflakes (F).
select = ["E4", "E7", "E9", "F"]
